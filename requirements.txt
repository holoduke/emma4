fastapi==0.115.0
uvicorn[standard]==0.32.0
httpx==0.27.2
pydantic==2.9.2
pydantic-settings==2.6.0
psutil==6.1.0
ultralytics==8.3.40
opencv-python-headless==4.10.0.84
pillow==11.0.0
# Vision-language detectors (GroundingDINO, OWLv2)
transformers>=4.46
# SD 1.5 inpainting + img2img with LCM LoRA
diffusers>=0.30
accelerate
peft
safetensors
# Apple Silicon native backend (faster Gemma 4 chat + vision via MLX)
# Only installs on macOS-arm64; safely no-op on other platforms.
mlx-vlm; sys_platform == "darwin" and platform_machine == "arm64"
# NOTE: torch is pulled in transitively by ultralytics. For NVIDIA CUDA, install
# the matching wheel first:  pip install torch --index-url https://download.pytorch.org/whl/cu121
# For macOS (Apple Silicon), the default PyPI wheel already includes MPS support.
